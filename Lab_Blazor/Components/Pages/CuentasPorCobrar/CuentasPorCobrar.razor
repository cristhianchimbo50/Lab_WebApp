@page "/cuentasporcobrar"
@rendermode InteractiveServer

@using Lab_Contracts.Pagos
@using Lab_Contracts.Ordenes
@using Lab_Blazor.Services.Pagos
@using Microsoft.AspNetCore.Components.Authorization
@using System.Security.Claims
@inject IPagosApiService PagosApi
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthProvider
@inject IJSRuntime JS

<PageTitle>Cuentas por Cobrar</PageTitle>

<AuthorizeView>
    <Authorized Context="authState">
        @{
            var user = authState.User;
            var rol = user.FindFirst(ClaimTypes.Role)?.Value
            ?? user.FindFirst("role")?.Value
            ?? user.FindFirst("roles")?.Value
            ?? "";

            bool puedeVer = rol is "administrador" or "recepcionista";
        }

        @if (!puedeVer)
        {
            <div class="d-flex justify-content-center align-items-center vh-100">
                <div class="card shadow-lg text-center border-0" style="max-width: 450px;">
                    <div class="card-body p-4">
                        <i class="bi bi-shield-lock text-primary fs-1 mb-3"></i>
                        <h5 class="fw-bold mb-2">Acceso no autorizado</h5>
                        <p class="text-muted mb-3">No tienes permisos suficientes para acceder a esta sección.</p>
                        <button class="btn btn-outline-primary" @onclick="VolverAtras">
                            <i class="bi bi-arrow-left"></i> Regresar
                        </button>
                    </div>
                </div>
            </div>
        }
        else
        {
            <h4 class="mb-4 d-flex align-items-center justify-content-between flex-wrap">
                <span><i class="bi bi-cash-coin text-primary fs-4 me-2"></i>Cuentas por Cobrar</span>
            </h4>

            <div class="card mb-3 shadow-sm border-0">
                <div class="card-body row g-3 align-items-end">
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Número de Orden</label>
                        <InputText class="form-control" @bind-Value="filtro.NumeroOrden" placeholder="ORD-00001" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Cédula</label>
                        <InputText class="form-control" @bind-Value="filtro.Cedula" placeholder="Cédula del paciente" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Nombre</label>
                        <InputText class="form-control" @bind-Value="filtro.NombrePaciente" placeholder="Nombre del paciente" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Estado de Pago</label>
                        <select class="form-select" @bind="filtro.EstadoPago">
                            <option value="">Todos</option>
                            <option value="PENDIENTE">Pendiente</option>
                            <option value="PAGADO">Pagado</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="card mb-3 shadow-sm border-0">
                <div class="card-body row g-3 align-items-end">
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Desde</label>
                        <InputDate class="form-control" @bind-Value="filtro.FechaInicio" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Hasta</label>
                        <InputDate class="form-control" @bind-Value="filtro.FechaFin" />
                    </div>
                    <div class="col-md-3 d-flex align-items-center mt-4">
                        <InputCheckbox @bind-Value="verAnulados" class="form-check-input me-2" />
                        <label class="form-check-label">Anulado</label>
                    </div>
                    <div class="col-md-3 d-flex align-items-center mt-4">
                        <InputCheckbox @bind-Value="verNoAnulados" class="form-check-input me-2" />
                        <label class="form-check-label">No Anulado</label>
                    </div>
                </div>
            </div>

            <div class="text-end mb-3">
                <button class="btn btn-primary me-2 shadow-sm" @onclick="BuscarAsync">
                    <i class="bi bi-search me-1"></i>Buscar
                </button>
                <button class="btn btn-outline-secondary shadow-sm" @onclick="LimpiarFiltros">
                    <i class="bi bi-eraser me-1"></i>Limpiar
                </button>
            </div>

            @if (ordenes == null)
            {
                <div class="alert alert-info text-center shadow-sm">
                    <i class="bi bi-info-circle me-2"></i>Presione <strong>Buscar</strong> para cargar los datos.
                </div>
            }
            else if (!ordenes.Any())
            {
                <div class="alert alert-warning text-center shadow-sm">
                    <i class="bi bi-exclamation-triangle me-2"></i>No se encontraron registros.
                </div>
            }
            else
            {
                <div class="d-flex flex-wrap justify-content-between align-items-center mb-3 gap-2">
                    <div class="text-muted small">
                        Mostrando @((CurrentPage - 1) * PageSize + 1)
                        - @(Math.Min(CurrentPage * PageSize, ordenes.Count))
                        de @ordenes.Count registros
                    </div>

                    <div class="d-flex align-items-center gap-2">
                        <label class="form-label mb-0 small">Registros por página:</label>
                        <select class="form-select form-select-sm w-auto" @bind="PageSize" @bind:after="(() => CurrentPage = 1)">
                            <option value="5">5</option>
                            <option value="10">10</option>
                            <option value="15">15</option>
                            <option value="20">20</option>
                        </select>

                        <div class="d-flex align-items-center ms-2">
                            <button class="btn btn-sm btn-outline-secondary me-1"
                                    disabled="@(CurrentPage == 1)"
                                    @onclick="(() => CambiarPagina(-1))">
                                <i class="bi bi-chevron-left"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-secondary"
                                    disabled="@(CurrentPage >= TotalPages)"
                                    @onclick="(() => CambiarPagina(1))">
                                <i class="bi bi-chevron-right"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <div class="tabla-fija shadow-sm">
                    <div class="tabla-body">
                        <table class="table align-middle tabla-pacientes">
                            <thead>
                                <tr>
                                    <th style="width:10%;">Número</th>
                                    <th style="width:10%;">Fecha</th>
                                    <th style="width:12%;">Cédula</th>
                                    <th style="width:18%;">Paciente</th>
                                    <th style="width:10%;">Total</th>
                                    <th style="width:10%;">Pagado</th>
                                    <th style="width:10%;">Saldo</th>
                                    <th style="width:10%;">Estado Pago</th>
                                    <th style="width:10%;">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var o in PaginatedOrdenes)
                                {
                                    <tr>
                                        <td>@o.NumeroOrden</td>
                                        <td>@o.FechaOrden.ToString("dd/MM/yyyy")</td>
                                        <td>@o.CedulaPaciente</td>
                                        <td>@o.NombrePaciente</td>
                                        <td>@o.Total.ToString("C")</td>
                                        <td>@o.TotalPagado.ToString("C")</td>
                                        <td class="fw-bold @(o.SaldoPendiente > 0 ? "text-danger" : "text-success")">@o.SaldoPendiente.ToString("C")</td>
                                        <td>
                                            <span class="badge bg-@(o.EstadoPago == "PAGADO" ? "success" : "warning text-dark")">@o.EstadoPago</span>
                                        </td>
                                        <td>
                                            <button class="btn btn-success btn-sm shadow-sm" @onclick="(() => IrACobro(o.CedulaPaciente))">
                                                <i class="bi bi-cash-coin me-1"></i> Cobrar
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            }
        }
    </Authorized>

    <NotAuthorized>
        <div class="d-flex justify-content-center align-items-center vh-100">
            <div class="card shadow-lg text-center border-0" style="max-width: 450px;">
                <div class="card-body p-4">
                    <i class="bi bi-person-x text-danger fs-1 mb-3"></i>
                    <h5 class="fw-bold mb-2">Debe iniciar sesión</h5>
                    <p class="text-muted mb-3">Inicie sesión con una cuenta autorizada para continuar.</p>
                    <NavLink href="/login" class="btn btn-primary">
                        <i class="bi bi-box-arrow-in-right"></i> Ir al Login
                    </NavLink>
                </div>
            </div>
        </div>
    </NotAuthorized>
</AuthorizeView>

@code {
    private List<OrdenDto>? ordenes;
    private PagoFiltroDto filtro = new();
    private bool verAnulados = true;
    private bool verNoAnulados = true;
    private int _pageSize = 10;
    private int PageSize { get => _pageSize; set { if (_pageSize != value) { _pageSize = value; CurrentPage = 1; } } }
    private int CurrentPage = 1;
    private int TotalPages => ordenes == null || ordenes.Count == 0 ? 1 : (int)Math.Ceiling(ordenes.Count / (double)PageSize);
    private IEnumerable<OrdenDto> PaginatedOrdenes => ordenes?.Skip((CurrentPage - 1) * PageSize).Take(PageSize) ?? Enumerable.Empty<OrdenDto>();

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            ordenes = null;
            StateHasChanged();
        }
    }

    private async Task BuscarAsync()
    {
        try
        {
            if (string.IsNullOrWhiteSpace(filtro.NumeroOrden) &&
                string.IsNullOrWhiteSpace(filtro.Cedula) &&
                string.IsNullOrWhiteSpace(filtro.NombrePaciente) &&
                string.IsNullOrWhiteSpace(filtro.EstadoPago) &&
                !filtro.FechaInicio.HasValue && !filtro.FechaFin.HasValue &&
                !verAnulados && !verNoAnulados)
            {
                await JS.InvokeVoidAsync("mostrarToastAsync", "Presione Buscar para cargar los datos.", "info");
                ordenes = null;
                return;
            }

            if (verAnulados && !verNoAnulados)
                filtro.IncluirAnulados = true;
            else if (!verAnulados && verNoAnulados)
                filtro.IncluirAnulados = false;
            else
                filtro.IncluirAnulados = null;

            ordenes = (await PagosApi.ListarCuentasPorCobrarAsync(filtro)).OrderByDescending(o => o.IdOrden).ToList();
            await JS.InvokeVoidAsync("mostrarToastAsync", "Datos cargados correctamente.", "success");
        }
        catch
        {
            await JS.InvokeVoidAsync("mostrarToastAsync", "Error al cargar los datos.", "danger");
        }
    }

    private async Task LimpiarFiltros()
    {
        filtro = new();
        verAnulados = true;
        verNoAnulados = true;
        ordenes = null;
        await JS.InvokeVoidAsync("mostrarToastAsync", "Filtros restablecidos.", "secondary");
    }

    private void CambiarPagina(int cambio)
    {
        CurrentPage = Math.Max(1, Math.Min(TotalPages, CurrentPage + cambio));
    }

    private void IrACobro(string? cedula)
    {
        if (!string.IsNullOrEmpty(cedula))
            Navigation.NavigateTo($"/cobros/{cedula}");
    }

    private async Task VolverAtras() => await JS.InvokeVoidAsync("history.back");
}
